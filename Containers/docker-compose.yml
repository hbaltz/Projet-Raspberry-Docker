
version: '2'
services:
  rok4: 
    image: rok4/rok4:arm-http
    # Exposed ports cannot be accessed publicly over the internet. (published ports are !)
    ports: 
      - 8080:8080
    volumes:
      - /mnt/Data/geodata/scan1000:/rok4/config/layers
      - /mnt/Data/geodata/bdortho:/rok4/config/layers
    