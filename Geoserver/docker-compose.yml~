version: "2.0"
services:
  tomcat:
    image: dordoka/rpi-tomcat
    volumes:
      - /mnt/Data/tomcat/webapps:/opt/tomcat/webapps
    ports:
      - "8081:8080" 
      - "8009:8009"
  geoserver:
    build: geoserver
    volumes:
      - /mnt/Data/geoserver/data:/data


